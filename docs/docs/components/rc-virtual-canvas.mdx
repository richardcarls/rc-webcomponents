---
title: rc-virtual-canvas
---

import { ApiTable } from '@site/src/components/ApiTable';
import { AtAGlance } from '@site/src/components/AtAGlance';
import { VirtualCanvasDemo } from '@site/src/components/ComponentDemos';

# rc-virtual-canvas

A virtualized canvas surface for large scrollable datasets and overlays.

<AtAGlance
  packageName="@rcarls/rc-virtual-canvas"
  tag="rc-virtual-canvas"
  native="Canvas and overlay content"
  state="Virtual scroll geometry"
  events={['rc-virtual-canvas-pointer', 'rc-virtual-canvas-render']}
  related={[{ label: 'Theme previews', href: '/guide/theme-previews' }]}
/>

## Installation

`sh
npm install @rcarls/rc-virtual-canvas
`

`sh
yarn add @rcarls/rc-virtual-canvas
`

`js
import '@rcarls/rc-virtual-canvas/define';
`

## Live demo

<VirtualCanvasDemo />

## Theming

The default demo mode shows the component without a package theme. Use the per-demo controls to compare inherited, light, and dark color schemes or to apply the optional Material theme only inside that demo frame.

## API

<ApiTable tag="rc-virtual-canvas" />
