---
title: rc-chip-set
---

import Tabs from '@theme/Tabs';
import TabItem from '@theme/TabItem';
import { ApiTable } from '@site/src/components/ApiTable';
import { AtAGlance } from '@site/src/components/AtAGlance';

# rc-chip-set

Chip set coordinator for arrow-key navigation across chip controls.

<AtAGlance
  packageName="@rcarls/rc-chip-set"
  tag="rc-chip-set"
  native="Consumer-authored chips, buttons, or links"
  state="Roving focus only"
  events={[]}
  related={[
    { label: 'rc-chip', href: '/components/rc-chip' },
    { label: 'Theme previews', href: '/guide/theme-previews' },
  ]}
/>

## Installation

<Tabs groupId="package-manager">
  <TabItem value="npm" label="npm">

```sh
npm install @rcarls/rc-chip-set
```

  </TabItem>
  <TabItem value="yarn" label="Yarn">

```sh
yarn add @rcarls/rc-chip-set
```

  </TabItem>
</Tabs>

```js
import '@rcarls/rc-chip-set/define';
```

## Markup

```html
<rc-chip-set label="Recipe filters">
  <rc-chip variant="filter"><button type="button">Quick</button></rc-chip>
  <rc-chip variant="filter"><button type="button">Vegetarian</button></rc-chip>
</rc-chip-set>
```

`rc-chip-set` uses toolbar semantics for one-tab-stop arrow-key navigation.
Selection state stays with the chip or native control.

## API

<ApiTable tag="rc-chip-set" />
