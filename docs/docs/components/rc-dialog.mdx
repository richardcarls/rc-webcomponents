---
title: rc-dialog
---

import Tabs from '@theme/Tabs';
import TabItem from '@theme/TabItem';
import { ApiTable } from '@site/src/components/ApiTable';
import { AtAGlance } from '@site/src/components/AtAGlance';
import { DialogDemo } from '@site/src/components/ComponentDemos';

# rc-dialog

Draggable, resizable wrapper for a native `<dialog>`, following the [WAI-ARIA Dialog Modal pattern](https://www.w3.org/WAI/ARIA/apg/patterns/dialog-modal/).

<AtAGlance
  packageName="@rcarls/rc-dialog"
  tag="rc-dialog"
  native="Requires a direct child native dialog"
  state="Open state follows the native dialog"
  events={[
    'rc-dialog-open',
    'rc-dialog-toggle',
    'rc-dialog-request-close',
    'rc-dialog-cancel',
    'rc-dialog-close',
  ]}
  related={[{ label: 'Theme previews', href: '/guide/theme-previews' }]}
/>

## Installation

<Tabs groupId="package-manager">
  <TabItem value="npm" label="npm">

```sh
npm install @rcarls/rc-dialog
```

  </TabItem>
  <TabItem value="yarn" label="Yarn">

```sh
yarn add @rcarls/rc-dialog
```

  </TabItem>
</Tabs>

```js
import '@rcarls/rc-dialog/define';
```

## Live demo

<DialogDemo />

## Theming

The default demo mode shows the component without a package theme. Use the shared preview controls on this page to compare inherited, light, and dark color schemes or to apply the optional Material theme only inside the demo frames.

## API

<ApiTable tag="rc-dialog" />
