---
'@rcarls/rc-switch': patch
---

Document all 28 `rc-switch` CSS custom properties in the generated API
reference, including the two track-size properties that have no built-in
default and must be set by a theme.
