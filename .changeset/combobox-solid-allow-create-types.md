---
'@rcarls/rc-webcomponents': patch
---

Fix the Solid JSX typing for `rc-combobox`'s `allow-create` attribute, which had drifted from
the 0.3.0 attribute rename, and add the missing `prop:allowCreate` property-binding entry.
