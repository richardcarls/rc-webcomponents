---
'@rcarls/rc-disclosure': minor
---

Fragment targeting is now always-on: `rc-disclosure` automatically opens and scrolls when the URL hash matches any id in its subtree. No `fragment` attribute required. The `fragment` attribute is deprecated and has no effect.
