---
title: Progressive enhancement
---

# Progressive enhancement

This Docusaurus spike keeps the project rule: components enhance native HTML instead of replacing it.

Native controls stay in the document for form submission, labels, pre-upgrade behavior, and assistive technology. The migrated demos intentionally keep their `<select>`, `<details>`, and `<summary>` elements in light DOM so Docusaurus styling conflicts can be evaluated honestly.
